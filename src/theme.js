import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
  palette: {
    main: '#0bb5ff',
    // primarylight: '#54cbff',
    // background: '#fff',
    // text: '#494949',
    // inactive: '#d8d8d8',
    // font: '#333',
    // fieldBgDisable: '#f7f7f7',
  },

  typography: {
    fontSize: 14,
    color: '#333',
    fontFamily: 'product-sans-regular, sans-serif',
  },
})

export default theme
