version: '3'

services:

  virujh-frontend:
    container_name: virujh-frontend
    build:
     context: .
    ports:
      - '80:80'
      - '443:443'
    networks:
      - virujh_redis-net

networks:
   virujh_redis-net:
    external: true